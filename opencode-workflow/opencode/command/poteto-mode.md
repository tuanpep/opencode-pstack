---
description: Explicit full-playbook mode for high-risk or process-intensive work.
agent: deep
---

Load the `poteto-mode` skill in full, including its Principles index. Match a playbook and copy its steps into the todo list before acting. Orchestrate from this session. Delegate units to `@research`, `@worker`, or `@expert`. Independent `@worker` units with disjoint write paths run in the same turn, up to three. Do not nest. Skip `how`, `architect`, and `arena` unless the shape is unknown or expensive to reverse. Keep changes minimal. After children return, spawn remaining ready siblings immediately, then prove the result with the narrowest meaningful check and a finite bash timeout. If a command or child times out, continue remaining work and report the unverified scope.

Task:
$ARGUMENTS
