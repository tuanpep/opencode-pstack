---
description: Explicit full-playbook mode for high-risk or process-intensive work.
agent: deep
---

Load the `poteto-mode` skill in full, including its Principles index. Match a playbook and copy its steps into the todo list before acting. Orchestrate from this session. Delegate units to `@research`, `@worker`, or `@expert`. Do not nest. Skip `how`, `architect`, and `arena` unless the shape is unknown or expensive to reverse. Keep changes minimal. After children return, prove the result with the narrowest meaningful check.

Task:
$ARGUMENTS
