---
description: Mine recent OpenCode sessions into AGENTS.md learned facts. Project-local by default.
agent: fox
---

Load the `continual-learning` skill. Delegate mining and AGENTS.md edits to `@learn`. Do not commit unless the arguments explicitly ask to commit.

Scope:
$ARGUMENTS
